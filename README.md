# pet-shelter

Quick API to practice testing and documentation

## from Anson

- what's included in `details-params` branch: GET end point `/details/:state`
- how to run the project:
  `node server.ts`
  if it throws error, try
  `npm run dev-w-flag`
